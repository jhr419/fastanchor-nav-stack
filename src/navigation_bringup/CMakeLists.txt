cmake_minimum_required(VERSION 3.8)
project(navigation_bringup)

find_package(ament_cmake REQUIRED)
find_package(ament_cmake_python REQUIRED)

ament_python_install_package(${PROJECT_NAME})

install(PROGRAMS
  scripts/waypoint_mission_manager
  scripts/go2_twist_bridge_service_manager
  DESTINATION lib/${PROJECT_NAME})
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})

if(BUILD_TESTING)
  find_package(ament_cmake_pytest REQUIRED)
  ament_add_pytest_test(test_launch_config test/test_launch_config.py)
  ament_add_pytest_test(test_waypoint_sequence test/test_waypoint_sequence.py)
endif()

ament_package()
